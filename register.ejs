<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <style>
        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        @keyframes buttonHover {
            0% { background: conic-gradient(from 180deg, #FFA07A, #FFD700, #32CD32, #FF69B4); }
            100% { background: conic-gradient(from 360deg, #FFA07A, #FFD700, #32CD32, #FF69B4); }
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            text-align: center;
            background: linear-gradient(135deg, #FFA07A, #FFD700, #32CD32, #FF69B4);
            background-size: 300% 300%;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            animation: gradient 5s ease infinite;
        }

        h1 {
            font-size: 2em;
            margin-bottom: 20px;
            color: #FFFFFF; /* White */
        }

        label {
            display: block;
            margin-top: 10px;
            font-weight: bold;
            color: #FFFFFF; /* White */
        }

        input {
            width: 70%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 20px;
            margin-right: 4px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background: #FFFFFF; /* White background for input fields */
            color: #333; /* Dark text color */
        }

        button {
            padding: 10px 20px;
            background: conic-gradient(from 180deg, #FFA07A, #FFD700, #32CD32, #FF69B4);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
            transition: background 0.5s;
        }

        button:hover {
            animation: buttonHover 2s linear infinite;
        }

        .error {
            color: #FF69B4; /* Pink */
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Register</h1>

        <% if (errorMessage) { %>
            <p class="error"><%= errorMessage %></p>
        <% } %>

        <form action="/register" method="POST">
            <label for="username">Username:</label>
            <input type="text" id="username" name="username" required>
            
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" required>
            
            <label for="phoneNumber">Phone Number:</label>
            <input type="text" id="phoneNumber" name="phoneNumber" required>
            
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" required>
            
            <label for="confirmPassword">Confirm Password:</label>
            <input type="password" id="confirmPassword" name="confirmPassword" required><br>
            
            <button type="submit">Register</button>
        </form>
    </div>
</body>
</html>
